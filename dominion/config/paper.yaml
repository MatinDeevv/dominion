aphelion:
  execution_mode: paper
  timezone: UTC
  symbol: XAUUSD
  paper:
    mode: live
    initial_capital: 10000.0
    symbol: XAUUSD
    hydra_checkpoint: ""
    warmup_bars: 200
    enable_tui: true
  mt5:
    login: 0
    password: "${MT5_PASSWORD}"
    server: "${MT5_SERVER}"
    terminal_path: "${MT5_TERMINAL}"
    timeout_ms: 10000
    retry_attempts: 3
    retry_delay_seconds: 5.0
